A simple command-line tool for managing an encrypted password safe.

Requires Python 3.  You also need the Python Cryptography Toolkit from
http://www.pycrypto.org/.

By default, passwords are stored in ~/.psafe/db.
